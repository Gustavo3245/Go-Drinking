services:
  db:
    image: postgres:15-alpine
    container_name: drinks_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mixology
    volumes:
      - ./src/main/kotlin/Api/db/tables:/docker-entrypoint-initdb.d
    ports:
      - "1234:1234"